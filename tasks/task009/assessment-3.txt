产物不满意：
1. internal_task_runner 的 README.md 触发器竞态防护章节 definition_snapshot 示例仍使用 info.definition.handler = 直接字段访问。文档展示了已废弃的内部操作模式，与通过公共方法替换 handler 的要求矛盾。用户参考文档后可能绕过 set_task_handler 直接操作字段导致锁失效。根因是测试重构后未同步更新文档旧示例。

过程不满意：
1. session-cot-3 声称添加动态 Handler 替换独立章节，最终总结列出为独立章节之一。但实际 README.md 完全不存在该章节，仅概览有单行要点，编辑命令可能静默失败。模型未回读验证就错误断言完成，重复了之前声称四章实际只有要点的虚假报告模式。正确做法是每次文档编辑后用 Read 工具回读验证落盘成功。
